import type * as Phaser from "phaser";
export declare class Sound {
    private url;
    constructor(url: string);
    play(config: Phaser.Types.Sound.SoundConfig | undefined): string;
    stop(): string;
}
